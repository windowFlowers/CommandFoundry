# AegisCopilot 项目规则 / Project Rules

## 后续任务必须积极使用子 agent

对本仓库后续的开发、修复、测试、打包、发布或审查任务，主 agent 必须在有可用并发槽位时主动委派至少一个边界清晰的子任务。适合委派的工作包括：

- 独立代码审查、回归测试和安全审计；
- 文档、README、发布说明和变更清单审查；
- 前端/后端/桌面端的并行实现或验证；
- 构建产物、版本号、依赖和发布范围审计。

主 agent 负责整合共享工作树、处理冲突、最终测试和交付，不得把最终集成判断完全交给子 agent。若没有可用槽位、任务极其简单，或平台不提供子 agent，必须在工作记录或交付说明中明确说明原因。

## Computer Use 规则

- 涉及 Windows 桌面 UI 时，先读取并遵循 `computer-use` 技能；只在真实 native app surface 可用时宣称完成 UI 验收。
- 不通过 UI 自动化终端、命令提示符或 PowerShell；不执行 AegisCopilot 生成的命令。
- 不把脚本烟测冒充为桌面 UI 验收；CUA 不可用时如实记录阻塞原因。

## 发布与安全

- 发布前必须审计 `git status`、`.gitignore`、密钥、依赖目录、缓存、运行数据库和日志。
- 安装包、`.exe` 和其他大二进制文件作为 GitHub Release asset，不直接提交到源码历史。
- 不提交 API Key、密码、Token、私钥、用户数据库、模型缓存或本机配置。
- 外部 push、Release 创建和资产上传前，必须明确核对目标仓库、分支、提交范围、标签、资产路径和校验哈希。
